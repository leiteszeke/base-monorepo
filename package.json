{
  "dependencies": {
    "tw-animate-css": "^1.3.0"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^2.3.0",
    "typescript": "5.8.3"
  },
  "engines": {
    "node": ">=20"
  },
  "name": "@leiteszeke/base-monorepo",
  "packageManager": "yarn@4.6.0",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "lint": "turbo lint"
  },
  "version": "0.0.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
